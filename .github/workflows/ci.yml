# Copyright (c) 2026 Mike Grier. All rights reserved.
name: ci

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.repository }}-${{ github.workflow }}-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

# These crates are Windows-only (every item is behind `cfg(windows)`), so CI
# builds, tests, and lints exclusively on Windows. Both the `--all-features`
# union and the minimal default (no-feature) configuration are exercised: the
# default set is a supported, published configuration, and a feature-union-only
# build can hide a missing `cfg` or an item that depends on an opt-in feature.
jobs:
  encoding:
    name: encoding sanity check
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
      - name: Run check-encoding.ps1
        shell: pwsh
        run: ./tools/check-encoding.ps1

  build-test:
    name: build + test
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: cargo build (all features)
        run: cargo build --workspace --all-targets --all-features --locked
      - name: cargo build (default features)
        run: cargo build --workspace --all-targets --locked
      - name: cargo test (all features)
        env:
          RUST_BACKTRACE: 1
          RUST_LIB_BACKTRACE: 1
        run: cargo test --workspace --all-features --locked --no-fail-fast
      - name: cargo test (default features)
        env:
          RUST_BACKTRACE: 1
          RUST_LIB_BACKTRACE: 1
        run: cargo test --workspace --locked --no-fail-fast

  fmt:
    name: rustfmt
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - name: cargo fmt --check
        run: cargo fmt --all --check

  clippy:
    name: clippy
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - name: cargo clippy (all features)
        run: cargo clippy --workspace --all-targets --all-features --locked -- -D warnings
      - name: cargo clippy (default features)
        run: cargo clippy --workspace --all-targets --locked -- -D warnings

  msrv:
    name: MSRV check 1.97
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@1.97.0
      - uses: Swatinem/rust-cache@v2
      - name: cargo check (MSRV)
        run: cargo check --workspace --all-targets --all-features --locked
